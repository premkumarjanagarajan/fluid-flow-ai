Run the FF Init skill from `skills/ff-init/ff-init.md` with force flag `true` (always full run when invoked via slash command).

This initializes the Fluid-Flow workspace: detects environment, validates workspace structure, loads environment variables, verifies MCP servers, and runs reverse engineering for brownfield repos.
