Read `orchestrator.md` from the `fluid-flow-ai` repository root and execute every instruction in it sequentially, in order, without skipping steps.

Do NOT jump ahead, shortcut, or bail out early — even if you encounter a blocker (e.g. auth failure, missing data). Surface blockers within the orchestrator flow, not instead of it.

Never auto-commit. Always wait for explicit user approval at every gate.
