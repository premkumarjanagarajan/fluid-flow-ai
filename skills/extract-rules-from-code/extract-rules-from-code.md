# Prompt: Extract reusable rules from an existing code pattern

Goal:
Create a `betsson-kb-docs` "pattern" document that explains how work is done in code today.

Inputs:
- Repo area / folder paths
- A few representative file paths
- Any constraints (jurisdictions, DS rules, API rules)

Output requirements:
- Produce a canonical betsson-kb-docs markdown file with:
  - Version/Owner/Applies To/Change Log headers
  - "Intent"
  - "When to use"
  - "Required steps"
  - "Do / Don't"
  - "Example paths in code"
  - "Common pitfalls"
  - "Open questions" (if any)

Do not invent missing details.
If information is missing, list what needs confirmation.

Input context:
<PASTE PATHS + EXAMPLES HERE>
