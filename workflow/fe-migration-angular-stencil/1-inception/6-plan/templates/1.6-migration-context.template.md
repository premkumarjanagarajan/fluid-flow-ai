# Migration Context — {WIDGET_NAME}

> This file is the handoff from the Inception phase to the Construction phase.
> Read this file first when starting Construction. Do NOT re-read Angular source files or Confluence — all needed context is here.
> This is a **temporary** document — delete after the implementation PR is merged.

## Key References

- **JIRA epic:** {JIRA_KEY}
- **Branch convention:** `feature/{JIRA_KEY}-migrate-{WIDGET_NAME}`
- **New widget name (kebab-case):** {WIDGET_NAME}
- **New widget name (PascalCase):** {PASCAL_CASE_WIDGET_NAME}
- **Target type:** {TARGET_TYPE}
- **Angular entry point:** {ENTRY_POINT}

## Component Breakdown

| Component | File | Responsibility | Parent |
|---|---|---|---|
| sb-xp-{widget} | src/components/sb-xp-{widget}/sb-xp-{widget}.tsx | Orchestrator — state, data fetch, Event Bus | (root) |
| {child} | {path} | {responsibility} | {parent} |

## Files to Create

- `{full path}` — {purpose}

## Reusability Decisions

| Item | Decision | Notes |
|---|---|---|
| {component/util} | {Reuse as-is / Adapt / Create new} | {reason} |

## BFF Summary

- **Fetch config endpoint:** {name only}
- **Fetch data endpoint:** {name only}
- **Response types to create:** {TypeScript type names}
- **Response types to reuse from libs/contracts:** {TypeScript type names}

## Typography Replacements

| Angular element | Genos class | Replacement |
|---|---|---|
| {element} | {genos-class} | `<fds-sb-typography variant="{variant}" tag="{tag}">` |

## Genos CSS Variables to Replace

| Variable | Replacement |
|---|---|
| `var(--genos-{name})` | {project token or documented gap} |

## Key Architectural Decisions

- {bullet list of choices}

## Full Plan Documents

- Architecture reference: `initiatives/{INITIATIVE_NAME}/artefacts/1.5-architecture.md`
- Implementation plan: `initiatives/{INITIATIVE_NAME}/artefacts/1.6-implementation-plan.md`
- Requirements: `initiatives/{INITIATIVE_NAME}/artefacts/1.4-requirements.md`
