# 1.2 Analysis Summary: {WIDGET_NAME}

## Widget Overview

- **Source**: {ENTRY_POINT}
- **Target**: {TARGET_TYPE}
- **Domain**: front-end
- **Saved analysis reused**: {yes — path / no}

## Component Tree

| Component | Type | Inputs | Outputs | Depth |
|-----------|------|--------|---------|-------|
| {name} | {root/child/service} | {list} | {list} | {0-3} |

## Data Flow

```mermaid
sequenceDiagram
%% Replace with actual data flow
```

## Services

| Service | Purpose | API Calls | State Management |
|---------|---------|-----------|-----------------|
| {name} | {purpose} | {endpoints} | {NgRx/local/none} |

## BFF Contracts

### {Endpoint Name}

- **Method**: {GET/POST}
- **Path**: {/api/path}
- **Request type**: {TypeName}
- **Response type**: {TypeName}
- **Existing in libs/contracts**: {yes/no}

## Dependencies

- **Angular-specific** (to be replaced): {list}
- **Reusable** (keep as-is): {list}
- **Third-party UI** (e.g. Swiper, Material): {list}

## Config Usage (to be omitted)

| Config Source | Value | MFE Replacement |
|---------------|-------|-----------------|
| {source} | {what it provides} | {prop/Event Bus/BFF/omit} |

## Typography Classes Found (→ replace with `fds-sb-typography`)

| Element | Genos Class(es) | Purpose | Replacement |
|---------|----------------|---------|-------------|
| `{element}` | `{genos-class}` | {text purpose} | `<fds-sb-typography variant="{variant}" tag="{tag}">` |

*(If none found: "No Genos typography classes found.")*

## Genos CSS Variables Found (→ MUST NOT be used in new architecture)

| Variable | File | Usage Context |
|----------|------|---------------|
| `var(--genos-{name})` | {file path} | {what it is used for} |

*(If none found: "No Genos CSS variables found.")*

## Analytics Tracking

| Event Name | Trigger Condition | Payload |
|------------|-------------------|---------|
| {name} | {when it fires} | {data sent} |

*(If no analytics found: "No analytics tracking found.")*

## Migration Notes

### What to omit
- {Angular pattern} — {reason to omit}

### What to replace with Event Bus
- {NgRx selector / service call} → `{sb-xp:event:name:v1}` — {context}

### What to adapt
- {Angular pattern} → {Stencil equivalent}
- Genos typography classes → `<fds-sb-typography variant="..." tag="...">`
- Genos CSS variables → project SCSS variables / design tokens

### Key UI sections (future Stencil child components)
- **{SectionName}** — {one sentence describing the UI section}
